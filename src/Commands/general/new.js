const { getStats } = require('../../lib/stats')

module.exports = {
    name: 'admin',
    aliases: ['ad'],
    category: 'devadmin',
    exp: 5,
    description: 'only admin',
    async execute(client, arg, M) {
    
        await M.reply(
            `f`
        )
    










        
    }
}
